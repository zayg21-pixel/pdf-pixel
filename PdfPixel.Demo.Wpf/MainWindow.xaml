<Window x:Class="PdfPixel.Wpf.Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PdfPixel.Wpf.Demo"
        xmlns:pdfPanel="clr-namespace:PdfPixel.PdfPanel;assembly=PdfPixel.PdfPanel"
        xmlns:wpfPdfPanel="clr-namespace:PdfPixel.PdfPanel.Wpf;assembly=PdfPixel.PdfPanel.Wpf"
        mc:Ignorable="d"
        ThemeMode="System"
        Title="PDF Pixel" Width="800" Height="600">
    <Window.Resources>
        <Style x:Key="SeparatorStyle" TargetType="Separator">
        </Style>
        <Style x:Key="PageTextBox" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="Red" />
        </Style>
    </Window.Resources>
        
    <Grid Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
        <Grid.DataContext>
            <local:MainWindowsViewModel />
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ToolBar ToolBarTray.IsLocked="True" Grid.Column="0">
                <ComboBox ItemsSource="{Binding PdfFiles}" SelectedItem="{Binding SelectedPdfFile}" MaxWidth="300" />
            </ToolBar>

            <ToolBar Grid.Column="1" HorizontalAlignment="Center" ToolBarTray.IsLocked="True">

                <Button Command="{Binding ZoomInCommand}" ToolTip="Zoom In (Ctrl++)" Cursor="Hand">
                    <TextBlock Text="&#xE8A3;" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="16" VerticalAlignment="Center" />
                </Button>

                <Button Command="{Binding ZoomOutCommand}" ToolTip="Zoom Out (Ctrl+-)" Cursor="Hand">
                    <TextBlock Text="&#xE71F;" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="16" VerticalAlignment="Center" />
                </Button>

                <Separator VerticalAlignment="Center" Height="20" Margin="4 0" />

                <TextBox Text="{Binding PageNumber, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center" MinWidth="30" />

                <TextBlock Text="of" VerticalAlignment="Center" Margin="0 0 4 0" />
                <TextBlock Text="{Binding Pages.Count}" VerticalAlignment="Center" />

                <Separator VerticalAlignment="Center" Height="20" Margin="4 0" />

                <Button Command="{Binding RotatePageCommand}" ToolTip="Rotate Current Page">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7AD;" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="16" Margin="0 0 4 0" VerticalAlignment="Center" />
                        <TextBlock Text="Rotate" />
                    </StackPanel>
                </Button>

                <Button Command="{Binding RotateAllPagesCommand}" ToolTip="Rotate All Pages">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7AD;" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="16" Margin="0 0 4 0" VerticalAlignment="Center"  />
                        <TextBlock Text="Rotate All" />
                    </StackPanel>
                </Button>

            </ToolBar>

            <ToolBar ToolBarTray.IsLocked="True" Grid.Column="2">
                <TextBlock Text="Scale:" VerticalAlignment="Center" Margin="0 0 4 0" />
                <ComboBox ItemsSource="{Binding AutoScaleModes}" SelectedItem="{Binding AutoScaleMode}" />
            </ToolBar>
        </Grid>

        <ScrollViewer Grid.Row="1" CanContentScroll="True" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <wpfPdfPanel:WpfPdfPanel x:Name="pdfPanel"
                                     CurrentPage="{Binding PageNumber}"
                                     Pages="{Binding Pages}"
                                     PageCornerRadius="8"
                                     AutoScaleMode="{Binding AutoScaleMode}"
                                     PanelInterface="{Binding PanelInterface}">
                <wpfPdfPanel:WpfPdfPanel.AnnotationToolTip>
                    <ToolTip Background="Transparent" BorderThickness="0">
                        <ToolTip.ContentTemplate>
                            <DataTemplate DataType="{x:Type pdfPanel:PdfAnnotationPopup}">
                                <ItemsControl ItemsSource="{Binding Messages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderBrush="Gray" CornerRadius="4" BorderThickness="1" Background="#EEEEEE" Margin="4">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Title}" Margin="4" FontSize="14" FontWeight="Bold" />
                                                    <TextBlock Text="{Binding Contents}" Margin="4" FontSize="12" />
                                                    <TextBlock Text="{Binding CreationDate}" Margin="4" FontSize="10" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </DataTemplate>
                        </ToolTip.ContentTemplate>
                    </ToolTip>
                </wpfPdfPanel:WpfPdfPanel.AnnotationToolTip>
            </wpfPdfPanel:WpfPdfPanel>
        </ScrollViewer>
    </Grid>
</Window>
